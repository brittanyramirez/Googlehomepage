// this is activating the google search button to work//
function googleSearch() {
    const query = document.getElementById("searchBar").value;
    alert("You Search for: " + query);
}
// this is making sure the lucky button is working//
function luckyButton() {
    alert("I'm feeling lucky!");
}